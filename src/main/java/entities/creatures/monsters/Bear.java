package entities.creatures.monsters;

import main.Handler;

public class Bear extends Monster {
    public Bear(Handler handler, float x, float y, int width, int height, int level, String name) {
        super(handler, x, y, width, height, level, name);
    }
}
